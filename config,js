// config.js
// The ONLY place that knows where the API lives.
// Update this if your Worker URL changes.

window.API_BASE = 'https://schintegration.ostroshile.workers.dev/api';
// With a custom domain it would look like:
// window.API_BASE = 'https://api.yourdomain.com/api';
